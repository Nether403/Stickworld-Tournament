export const HOOKLINE_INSTRUCTIONS = `Hold to attach a rope to an anchor. Release to let go. Aim with the pointer, or press Space / E to attach toward the nearest forward anchor. WASD or arrow keys nudge aim. Practice can pause; ranked cannot.

Gates use both colour and shape so colour is never the only cue: chevron, diamond, plus, and square.`;

export const PICKAXE_INSTRUCTIONS = `Hold to bite a ledge with the pickaxe. Release to swing. Aim with the pointer, or press Space / E toward the nearest ledge above. WASD or arrow keys nudge aim. Practice can pause; ranked cannot.

Checkpoints use both colour and shape so colour is never the only cue: chevron, diamond, plus, and square, repeating up the shaft.`;

export const LAUNCH_LAB_INSTRUCTIONS = `Drag to set aim (horizontal) and power (vertical). Press Space to launch; pointer-up does not launch. Hold Shift to tuck in flight. Three launches, all count. Practice can pause; ranked cannot.

Rings use both colour and the same rectangular silhouette so colour is never the only cue.`;

export const ARCHERY_INSTRUCTIONS = `Aim from the torso toward the pointer; drag distance sets draw. Press Space to fire. One arrow at a time; it returns to the string after it sleeps or leaves the course. Practice can pause; ranked cannot.

Targets use both colour and size so colour is never the only cue.`;

export const HAMMER_INSTRUCTIONS = `Hold D or Right to spin the hammer. Press Space to release. Touch: hold to spin, release the hold to throw. Three throws, all count. Practice can pause; ranked cannot.

Gates use both colour and order so colour is never the only cue.`;

export const POGO_INSTRUCTIONS = `Auto-bounce on ledges. Lean with A/D or by moving the pointer. The week's seed is the tower. Practice can pause; ranked cannot.

Ledges use both colour and motion so colour is never the only cue: static ledges are muted, movers are accent.`;

export const ROOFTOP_INSTRUCTIONS = `Hold Right to run, Left to back up, Space to jump, C to slide. Touch: tap the right half to run and jump. Practice can pause; ranked cannot.

Roofs use both height and lintel beams so colour is never the only cue.`;

export const BIKE_INSTRUCTIONS = `Hold Right to throttle, Left to brake. Move the pointer to lean. Touch: right half throttles, left half brakes. Practice can pause; ranked cannot.

Ramps use both slope and order so colour is never the only cue.`;

export const CARGO_INSTRUCTIONS = `Aim with the pointer. Hold Space or the pointer to hook the carrier to a post. The crate hitch is always on. Practice can pause; ranked cannot.

Posts use both position and size so colour is never the only cue.`;

export const DEMOLITION_INSTRUCTIONS = `Drag to aim from the gantry; vertical drag sets power. Press Space to dive. Three dives, all count. Practice can pause; ranked cannot.

Storeys use both height and value so colour is never the only cue.`;
